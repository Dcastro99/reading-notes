# **Bearer Authorization**

> ## Intro to JWT

1. What is a JSON Web Token (JWT)?

- a compact and self-contained way for securely transmitting information between parties as a JSON object.

2. When should we use JSON Web Tokens?

- When we need to authorize a user or exchange information

3. Claims are expected in which structural component of a JWT?

- Payload

> ## Are JWT's Secure?

1. If I get a JWT and I can decode the payload, how can we call that secure?

- The "secret" is secure. JWt is a validation key

2. If sending a JWT, what must sender and receiver both know? Hint, it’s appended in the signature.

- Secret

3. Explain how concatenated content and secret can be sent and received securely to a non-technical recruiter.

- Someone has a ticket to a concert. THe venue checks to see if the ticket number exsits as a valid ticket. When it verifies ticket numbers macth, you can enter.

> ## JWT's Explained

1. Why use JWT?

- Digitally signed, verified, and trusted

2. JWT is Compact and self-contained. Describe how this is useful to a non-technical friend.

- It is used to transfer sensitive data.

3. What are the three components (the structure) of a JWT signature?

- Header / Payload / Signature

> ### Resources

- [JWT](https://canvas.instructure.com/courses/4898196/discussion_topics/15038329)


## Things I want to know more about